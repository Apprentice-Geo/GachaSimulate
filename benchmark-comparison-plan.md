# C++ / Python 公平性能对比计划

## 状态

计划已确认；本轮不改 benchmark 实现、不执行测量。

## 目的

为后续 PR 提供可复现、可解释的 Python 与 C++ 性能结论。结论必须区分：

1. 已编译规则下的核心模拟吞吐；
2. 用户从启动命令到产物落盘的端到端耗时。

当前报告中的倍率不能直接作为上述任一结论：C++ 计时从可执行程序内部开始且会写 GSR，Python 计时包含多进程创建并保留更多结果数组。

## 非目标

- 不要求两端使用相同 RNG 算法或逐 run 得到相同样本序列。
- 不将 Python 多进程和 C++ 多线程称作相同的并行机制。
- 不设置 CI 性能门槛。

## 实验 A：核心模拟吞吐

### 计时边界

- Python：`RuntimeContext` 已构建后，进入固定 runs 批量模拟开始计时；不写文件。
- C++：`RuntimeProgram` 已加载后，进入固定 runs 批量模拟开始计时；不写 GSR。
- 两端都产生相同的最小结果契约：每 run 的 `draw_count`、可选 `cost`、`termination_reason`，以及汇总值。
- Python 基准路径不得额外保留 `lifetime_acquired`；否则两端的结果负载不同。

### 规模和并行度

对六个既有 benchmark case 分别测量：

| runs | Python workers | C++ threads |
| ---: | ---: | ---: |
| 100 | 1 | 1 |
| 10,000 | 1、16 | 1、16 |
| 100,000 | 1、16 | 1、16 |
| 1,000,000 | 1、16 | 1、16 |

16 是固定实验参数，即使机器逻辑核数更多也不自动扩大。

### 执行方法

- 每个数据点先预热 2 次，再测量 7 次。
- 使用单调时钟，报告中位数、最小值、最大值；不以单次结果下结论。
- 多进程模式需拆分并报告 worker 创建/IPC 的成本，或提供预启动 worker pool 的稳态测量；两者不可混在一个数字中。
- 每次测量前记录机器处于接电、非省电模式，且没有可避免的重负载任务。

## 实验 B：端到端任务耗时

### 计时边界

由父进程在启动命令前开始计时，在子进程退出后结束计时。两端均包含：

- 进程启动；
- 配置加载与编译；
- 批量模拟；
- 结果写入；
- 进程退出。

Python 的 `.npz + _visualize.json` 与 C++ 的 GSR 不相同。报告必须写明各自产物、字段与压缩方式；只能称为“当前管道端到端耗时”，不能归因成纯模拟效率。

### 规模和报告

使用 10,000、100,000、1,000,000 runs，单并行度与 16 并行度各一组。预热、重复次数和统计方式与实验 A 相同。

## 正确性门槛

在计时之前，每个 case 和并行度必须通过：

- fixed-runs 精确返回请求 run 数；
- target-total-draw 达到或超过目标；
- 同一实现、相同 seed/并行参数重复运行保持确定；
- `draw_count`、cost 存在性、终止原因及 GSR/结果格式满足共享契约；
- 两端满足相同规则不变量，并对大样本的终止原因/抽数分布做统计合理性检查。

RNG 实现不同，因此不将逐样本相等作为跨语言正确性门槛。

## 归档要求

每份 PR 报告记录：

- Git commit；CPU 型号、逻辑核数、内存、WSL/Linux 版本与电源模式；
- Python、NumPy、Node、Clang、CMake 版本，及 C++ 构建类型；
- case 名称与配置哈希；
- seed、runs、workers/threads、预热与重复次数；
- 完整计时边界、结果格式及原始中位数/范围。

PR 文案只陈述与表格匹配的结论，例如“在实验 A 的 100,000 runs、16 并行度下，C++ 的核心模拟中位耗时为 X，Python 为 Y”。不得把端到端表的倍率写成纯计算倍率。

## 后续实现步骤

1. 为 Python 增加仅收集最小结果契约的 benchmark 路径；为 C++ benchmark 增加不加载 IR、不写 GSR 的核心计时模式。
2. 为两端增加父进程端到端驱动和原始结果 JSON/CSV 保存。
3. 将预热、重复、规模、固定 16 并行度及环境采集固化到单一驱动。
4. 先运行正确性门槛，再运行实验 A、实验 B，最后生成可提交 Markdown 报告。

## 验收标准

- 任何倍率都能追溯到明确的计时边界和原始样本。
- 核心吞吐与端到端结果分表展示，且不互相替代。
- PR 审阅者可在同类 WSL/Linux + Clang 环境中用文档命令复现。
