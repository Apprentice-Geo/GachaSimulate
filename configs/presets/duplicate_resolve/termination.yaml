retained_items:
  - reward: 1
termination_rule:
  condition:
    op: OR
    children:
      - check: reward >= 2
        actions: terminate collection_goal
      - check: draw_count >= 100
        actions: terminate draw_cap
