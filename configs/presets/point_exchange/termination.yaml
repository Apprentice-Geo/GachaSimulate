retained_items:
  - exchange: 1
termination_rule:
  condition:
    op: OR
    children:
      - check: exchange >= 3
        actions: terminate exchange_goal
      - check: draw_count >= 100
        actions: terminate draw_cap
