### 项目定位

构建一个**概率收益累积型抽卡蒙特卡洛仿真系统**，用于产出“获取最大奖所需成本分布”的视频素材。

---

## 一、数学模型

单抽收益为离散随机变量：

[
X = v_i \quad (概率 p_i)
]

不断抽取直到：

[
\sum X_i \ge C
]

这是**更新过程的首达时间问题**。

---

## 二、数据流

```
JSON配置
   ↓
构建概率模型 (CDF)
   ↓
固定玩家样本数的完整模拟
   ↓
得到结构化玩家结果数组
   ↓
统计分析
   ↓
可视化输出
   ↓
数据与图像归档
```

标题中的“千万次模拟”来自最终累计抽数，而非停止条件。

---

## 三、模块结构

| 模块               | 职责                       |
| ---------------- | ------------------------ |
| `config.py`      | 读取 JSON，构建概率数组与 CDF，校验参数 |
| `montecarlo.py`  | 执行仿真，生成玩家级样本数据           |
| `dataprocess.py` | 统计分析与分布计算                |
| `visualize.py`   | 生成图像文件                   |

数据流单向，不交叉职责。

---

## 四、仿真核心逻辑

1. 使用 `np.random.default_rng()`（PCG64）
2. 生成 (r \in [0,1))
3. 用 `searchsorted(CDF, r)` 决定掉落
4. 累加代币直到达到目标

---

## 五、结果存储结构（每个玩家一条）

| 字段         | 含义           |
| ---------- | ------------ |
| pulls      | 达标所需抽数       |
| currency   | 最终累计代币       |
| overflow   | 超额代币（最后一抽浪费） |
| high_count | 高价值掉落次数      |

---

## 六、统计输出指标

* 平均抽数
* 中位数
* 50 / 75 / 95 / 5 分位
* 最大值
* 单抽期望收益（理论）
* 理论 vs 模拟均值对照

---

## 七、可视化内容

1. 抽数分布直方图
2. CDF 成本概率曲线
3. 多玩家代币增长轨迹
4. 理论期望线对比

---

## 八、NumPy 使用核心

* 数组与结构化数组
* `default_rng()`
* `cumsum()`
* `searchsorted()`
* `percentile()` 等统计函数

---

