关于物品附属行为，需要支持触发特定池子抽取和物品分解行为
池子是立刻触发，但是物品分解可能需要延后
可考虑在runtime设置专门的数组记录池子、分解信息，使用与item相同的index记录，对执行效率也有提升
设置resolve运行时数组

通过为上述行为定义action，可支持多种触发条件挂载，不过对于执行或许需要设计
action:{type: }
type:
- AddItem
- ReduceItem
- PoolChange
- DrawPool
- Termination
- ...

对于每一个type，继承基本的action类，各自补充执行需要的信息

这样设计可使得所有物品操作都由action完成
需要预先生成引擎可使用的事件，直接供给引擎调用，从而引擎不需要构造action

如此修改以后，支持：
- 抽取直到满足指定终止树的条件
- 行为：池子切换
- 行为：物品分解
- 行为：触发池子

对于可穷举的池子数，该机制应该足够。但是对于池子数量过多的情况不支持，不过目前没见过不同概率池子数量过多的情况
目前计划将抽取行为均使用action驱动,引擎变为action执行器以及终止条件检查器
那么主池的抽取要怎么编排呢,使用索引对对应的action进行execute
这样设计就统一了抽取行为的来源

对于Action的执行,可使用队列存储,当执行完毕一个action以后检查队列情况,执行其中的action.
那么有无必须立即执行的action呢,应该没有

对于抽取流程,每一次抽取应该为:
抽取
里程碑检查
分解检查,将非protected的物品全部分解
终止条件检查
执行队列中的action

为了优化时间复杂度,监控protected物品的数量,只有出现变化时才检查condition_tree判断是否终止

mian中为了给visualize需要的数据,需要额外记录多次模拟中需要的数据

对于Action的执行,如果在某个Action执行中触发,则加入队列中等待执行
先完成对wuxiang的修改

models定义builder输出的数据结构
builder把DSL变为Python的数据结构
engine使用builder给出的上下文运行

能否跳过parser呢，parser原语义应该是语法检查，但是DSL的语法自己实现，没有所谓检查
并且实际上也可以直接输出为int索引，按照物品，池子，规则，终止树的顺序编译即可
所以：删除parser
在DSL语法稳定以后,考虑增加Schema进行基础的JSON语法校验

变量命名：
驼峰：类名
大写字母下划线链接：常量
小写字母下划线链接：除去以上两个