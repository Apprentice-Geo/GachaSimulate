# Electron 模拟 MVP 检查问题清单

## 说明

本文保留 `electron_simulation_mvp_plan.md` 阶段六及之前发现问题时的描述，并在每项下记录后续处理状态和结果。

优先级定义：

- P0：可能破坏核心流程、遗留子进程或导致应用无法继续操作，应优先处理。
- P1：实现未完全满足既定契约，或缺少关键验证，建议在 MVP 收尾前处理。
- P2：主要影响可使用性、界面一致性和长期维护，可在核心问题完成后处理。

## P0：核心流程问题

### ESM-001：模拟失败后表单消失，无法直接修改参数重试

- 类型：可使用性、状态管理
- 位置：`src/renderer/App.tsx`
- 现状：配置扫描错误和模拟运行错误共用同一个 `error` 状态。只要 `error` 非空，页面就只渲染错误文本，不再渲染模拟表单。
- 影响：参数错误、Python 错误或启动失败后，用户无法在当前页面修正输入并重试。
- 期望：只有配置扫描失败阻断表单；模拟错误应在表单内展示，并允许用户修改参数后再次运行。
- 处理状态：已完成
- 处理结果：Renderer 已拆分配置扫描错误和模拟操作错误；模拟失败后表单保持可用，可修改参数重试。

**个人认为应该增加一个统一的报错窗口，用于不同报错信息的统一显示**

### ESM-002：JSONL 协议错误未终止完整进程树

- 类型：进程生命周期、可靠性
- 位置：`src/main/simulation.ts`
- 现状：JSONL 解析失败等致命协议错误进入 `fail()`，但只调用 `child.kill()`。
- 影响：Windows 多进程模拟可能只终止 `uv` 或 Python 主进程，遗留 worker 进程。
- 期望：所有需要强制终止模拟的路径复用同一个进程树终止实现。
- 处理状态：已完成
- 处理结果：协议错误和用户取消已复用同一进程树终止实现；Windows 使用 `taskkill /T /F`，其他平台发送 `SIGTERM`。

**这部分我不了解，需要给出方案和信息让我选择**

### ESM-003：取消命令失败可能使任务永久停留在 cancelling

- 类型：进程生命周期、退出安全
- 位置：`src/main/simulation.ts`、`src/main/index.ts`
- 现状：Windows `taskkill` 的启动错误和退出结果没有处理；任务在执行命令前已经进入 `cancelling`。关闭窗口时会等待任务进入终态。
- 影响：取消失败后 UI 无法再次取消或启动任务；关闭应用也可能一直等待。
- 期望：处理 `taskkill` 失败，向 Renderer 报告明确错误并保证任务进入可恢复的终态；关闭流程不能无限等待。
- 处理状态：已完成
- 处理结果：`taskkill` 错误和进程关闭超时会恢复可重试状态并报告错误；关闭窗口失败时保留窗口并显示错误对话框。

**这个问题的核心应该是没有对 taskkill 的结果进行处理就提前进行了状态转移，需要调查 taskkill 的结果如何查看**

## P1：契约与验证问题

### ESM-004：main 未确认 termination 属于 manifest 声明项

- 类型：IPC 边界、配置契约
- 位置：`src/main/index.ts`、`src/main/simulation.ts`
- 现状：启动模拟时只确认配置 ID 存在，随后接受配置目录内任意存在的 YAML 文件。
- 影响：Renderer 请求没有完全按 manifest 白名单重新验证，不符合计划中的 main 二次校验要求。
- 期望：启动前确认配置 ID 与 termination 文件名的组合存在于扫描结果中。
- 处理状态：已完成
- 处理结果：main 启动前按扫描结果精确校验配置 ID 和 manifest 声明的 termination 文件名；实际目录和 YAML 内容仍由模拟任务二次校验。

**确实是个问题，需要加校验**

### ESM-005：Electron 自动化测试未覆盖计划中的关键行为

- 类型：可维护性、回归保障
- 位置：`src/main/simulation.test.ts`
- 现状：现有 Electron 测试只覆盖请求校验、单行 JSONL 解析和进程结果纯函数。
- 缺少覆盖：
  - manifest 正常、损坏、ID 不匹配和路径穿越；
  - 预置配置初始化不覆盖已有目录；
  - 重复启动保护和真实任务状态变化；
  - 取消成功、取消失败和进程退出；
  - `completed`、`error` 与退出码组合；
  - 文件选择内容进入现有可视化校验流程。
- 影响：阶段三、五和六的关键边界可能在现有检查全部通过时仍发生回归。
- 期望：补充最小的行为测试，优先保护进程生命周期和 IPC 信任边界。
- 处理状态：已完成
- 处理结果：配置扫描、manifest 选择、预置初始化、任务终态/退出码、重复启动、取消及可视化文件校验链均已有行为测试；`test:simulation` 已接入 CI 和 push 前矩阵。

**补充缺失的校验即可**

### ESM-006：取消按钮未处理 IPC 拒绝

- 类型：错误处理、可使用性
- 位置：`src/renderer/App.tsx`
- 现状：取消按钮直接调用 `cancelSimulation()`，没有捕获 Promise rejection。
- 影响：main 取消失败时可能产生未处理拒绝，界面也不会向用户解释失败原因。
- 期望：与启动模拟、打开结果目录使用一致的错误处理方式。
- 处理状态：已完成
- 处理结果：取消 IPC rejection 已由 Renderer 捕获，并显示在模拟表单的操作错误区域。

**先给我解释一下，不太懂这里**

### ESM-007：worker 上限只在提交后校验

- 类型：表单交互、输入校验
- 位置：`src/renderer/App.tsx`、`src/shared/simulation.ts`
- 现状：Renderer 只设置最小值，没有展示或限制系统逻辑 CPU 上限。
- 影响：用户只能在提交后从错误文本中得知允许范围。
- 期望：Renderer 获得并显示 worker 上限，同时保留 main 的最终校验。
- 处理状态：已完成
- 处理结果：`DesktopApi.getLogicalCpuCount()` 通过 IPC 提供逻辑 CPU 数；Renderer 显示 `1–N`、设置输入上限并在提交前复用共享校验，main 保留最终校验。

**确实应该修复，期望的设想合理**

### ESM-008：cost 指标是否可用没有提前反馈

- 类型：表单交互、配置能力展示
- 位置：`src/renderer/App.tsx`、配置扫描返回结构
- 现状：所有配置都允许选择 cost，缺少 cost item 时只能在启动后报错。
- 影响：用户可以选择已知无法运行的组合，增加无效操作。
- 期望：配置列表提供必要的能力信息，缺少 cost 时禁用该选项或显示明确说明。
- 处理状态：已延后
- 处理结果：由配置仓库阶段引入 `metrics` manifest 契约及发布/安装校验；本轮不修改现有 manifest 或 `InstalledConfig`，main 继续校验实际配置。

**确实是问题，但是目前不能处理，留到后续配置仓库部分**

## P2：界面与维护体验问题

### ESM-009：运行状态和进度单位直接显示内部英文值

- 类型：UI 一致性
- 位置：`src/renderer/App.tsx`
- 现状：界面直接显示 `starting`、`running`、`saving`、`runs` 和 `draws`。
- 影响：与其余中文界面不一致，并暴露内部协议表达。
- 期望：在 Renderer 建立最小展示映射，不修改底层协议值。

### ESM-010：进度反馈只有原始数字

- 类型：可使用性、信息层级
- 位置：`src/renderer/App.tsx`、`src/renderer/styles.css`
- 现状：运行期间只显示 `completed/total` 文本，没有百分比、进度条或当前阶段说明。
- 影响：长时间运行时难以快速判断进度和当前工作阶段。
- 期望：复用现有状态和 progress 事件增加清晰的阶段文案及简单进度条。

**目前进度反馈很慢，需要先解决反馈即时性问题再考虑美观化，该问题可以暂缓**

### ESM-011：表单术语中英文混用

- 类型：UI 一致性
- 位置：`src/renderer/App.tsx`
- 现状：配置和目标使用中文，但 `Seed`、`Workers`、`Metric` 使用英文。
- 影响：界面语言和理解成本不一致。
- 期望：统一为中文，并在需要时保留英文术语作为辅助说明。

**可以统一，但是需要讨论使用什么词汇**

### ESM-012：结果路径展示产生较大视觉噪声

- 类型：UI 美观度、信息呈现
- 位置：`src/renderer/App.tsx`、`src/renderer/styles.css`
- 现状：完成后直接展示完整绝对路径。
- 影响：长路径占据较大空间，弱化状态与主要操作按钮。
- 期望：默认展示文件名或截断路径，完整路径通过 title、复制操作或辅助区域提供。

**先改为只展示结果文件名**

### ESM-013：模拟表单页面标题占用空间偏大

- 类型：UI 美观度、布局
- 位置：`src/renderer/styles.css`
- 现状：模拟页复用占位页标题样式，标题字号最高可达 `76px`。
- 影响：数据操作页面的首屏空间利用率偏低，表单与状态信息被向下推移。
- 期望：区分工作页面与占位页面的标题尺度。

**减小标题大小，对”配置仓库“界面的标题同样需要减少，此外目前的”目标“设置项目空间利用率很差，它可以移动到页面右端，和当前的配置选择和终止条件选择对齐**

### ESM-014：侧边栏拖拽宽度实现相对当前需求偏复杂

- 类型：可维护性、操作合理度
- 位置：`src/renderer/App.tsx`、`src/renderer/styles.css`
- 现状：三个固定导航项使用了指针捕获、键盘调节和连续宽度状态等完整拖拽逻辑。
- 影响：增加状态、事件处理和可访问性维护成本，但对当前 MVP 的实际收益有限。
- 期望：评估改为固定宽度或简单展开/折叠；只有出现明确的连续调宽需求时再保留拖拽。

**可以改为固定窄宽度，并在图标下面标注文字，文字需要一行写完，因此最窄宽度可能需要调整**

## 当前检查基线

本轮修复完成后实际执行并通过：

- `pnpm run test:simulation`：14 项通过。
- `pnpm run test:visualize:cdf`：17 项通过。
- `pnpm run format:check`：通过。
- `pnpm run lint`：通过。
- `pnpm run typecheck`：通过。
- `pnpm run build`：通过。
- `pnpm run test:e2e`：11 项通过。

本轮未执行 Python 检查；改动未涉及 Python 实现或配置语义。
