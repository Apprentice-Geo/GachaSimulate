[project]
name = "simulate"
version = "0.1.0"
description = "Monte Carlo Gacha Simulator"
requires-python = ">=3.12"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]