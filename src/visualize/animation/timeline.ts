export const ANIMATION_TOTAL_MS = 3000;
export const EXPORT_BUFFER_MS = 500;

export const ANIMATION_TIMELINE = {
  PAGE_IN_MS: 300,
  CURVE_DELAY_MS: 300,
  CURVE_DURATION_MS: 1500,
  MARKER_DELAY_MS: 1200,
  MARKER_DURATION_MS: 1000,
  METRIC_DELAY_MS: 1800,
  METRIC_DURATION_MS: 800,
  FOOTER_DELAY_MS: 2400,
  FOOTER_DURATION_MS: 600,
} as const;
